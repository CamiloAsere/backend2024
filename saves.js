
/*

const strategiesA = {
  strategyA1: StrategyA1,
  strategyA2: StrategyA2
};

const currentStrategyA = { value: 'strategyA1' };

app.get('/routeA', handleRequest(strategiesA, currentStrategyA));
app.get('/change-strategy-a/:strategy', handleChangeStrategy(strategiesA, currentStrategyA));


const strategiesB = {
  strategyB1: StrategyB1,
  strategyB2: StrategyB2
};

const currentStrategyB = { value: 'strategyB1' };

app.get('/routeB', handleRequest(strategiesB, currentStrategyB));
app.get('/change-strategy-b/:strategy', handleChangeStrategy(strategiesB, currentStrategyB));
*/